{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "in=$(cat); printf '%s' \"$in\" | grep -qE 'git[[:space:]]+add[^\"]*(\\.env|secrets/|google-credentials)' && { echo 'Blocked: refusing to \"git add\" secrets (.env / secrets/ / google-credentials). Keep them gitignored.' >&2; exit 2; }; exit 0"
          }
        ]
      }
    ]
  }
}
